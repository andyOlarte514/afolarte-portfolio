---
phase: 05-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/atoms/ContactIconButton.tsx
  - src/components/atoms/ContactIconButton.test.tsx
autonomous: true
requirements:
  - SEO-02
  - SEO-03
must_haves:
  truths:
    - "Keyboard users tab through ContactIconButton controls with exactly ONE focus stop per button (not two)"
    - "Visible focus ring appears on ContactIconButton when focused via keyboard"
    - "All icon-only contact buttons have aria-label (verified in existing tests)"
  artifacts:
    - path: "src/components/atoms/ContactIconButton.tsx"
      provides: "outer <a> with focus ring classes; inner Button with tabIndex={-1}"
      contains: "tabIndex={-1}"
    - path: "src/components/atoms/ContactIconButton.test.tsx"
      provides: "test asserting inner button has tabIndex -1 (prevents double focus stop)"
      contains: "tabIndex"
  key_links:
    - from: "src/components/atoms/ContactIconButton.tsx outer <a>"
      to: "keyboard tab order"
      via: "focus-visible:ring-2 focus-visible:ring-indigo-500 focus-visible:ring-offset-2 rounded-full on <a>"
      pattern: "focus-visible:ring-2"
    - from: "src/components/atoms/ContactIconButton.tsx inner Button"
      to: "removed from tab order"
      via: "tabIndex={-1}"
      pattern: "tabIndex=\\{-1\\}"
---

<objective>
Fix the double focus stop in ContactIconButton: the outer <a> and inner shadcn Button are both focusable, creating two consecutive tab stops for a single interactive control (WCAG 2.1 SC 2.4.3 violation). Resolution: add tabIndex={-1} to the inner Button and a visible focus ring to the outer <a>.

Purpose: Keyboard users navigate the contact section without getting stuck hitting the same button twice. The fix aligns with the NavLink focus ring pattern already established in Phase 1.
Output: ContactIconButton.tsx with single-focus-stop fix; ContactIconButton.test.tsx updated with tabIndex assertion.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-PATTERNS.md

<interfaces>
<!-- Current state of ContactIconButton — read before modifying -->

From src/components/atoms/ContactIconButton.tsx (current JSX, lines 16-22):
```tsx
<a href={href} aria-label={ariaLabel} target="_blank" rel="noopener noreferrer">
  <Button variant="outline" size="icon" className="h-11 w-11 rounded-full">
    {icon}
  </Button>
</a>
```

Problem: both <a> and Base UI Button are focusable — two consecutive tab stops for one control.

From src/components/atoms/NavLink.tsx (focus ring pattern to copy):
```tsx
className={[
  "relative py-1 text-base transition-colors",
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 focus-visible:ring-offset-2",
  // ...
```

From src/components/atoms/ContactIconButton.test.tsx (current 6 tests):
All 6 existing tests query container.firstChild (the <a>) — they pass unchanged after the fix.
One new test must be added: inner Button has tabIndex -1.

Test pattern to add:
```typescript
it("inner button has tabIndex -1 to prevent double focus stop", () => {
  const { container } = render(
    <ContactIconButton href="mailto:test@test.com" ariaLabel="Test label" icon={testIcon} />,
  );
  const anchor = container.firstChild as HTMLAnchorElement;
  const button = anchor.querySelector("button");
  expect(button).toHaveAttribute("tabindex", "-1");
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix ContactIconButton double focus stop</name>
  <files>src/components/atoms/ContactIconButton.tsx, src/components/atoms/ContactIconButton.test.tsx</files>
  <read_first>
    src/components/atoms/ContactIconButton.tsx — read full file (24 lines) before editing
    src/components/atoms/ContactIconButton.test.tsx — read full test file (57 lines) before editing
    src/components/atoms/NavLink.tsx — lines 27-32, the exact focus-visible class string to copy for the outer <a>
  </read_first>
  <behavior>
    - Existing test "root element is an anchor" passes unchanged
    - Existing test "href attribute matches prop" passes unchanged
    - Existing test "aria-label attribute matches ariaLabel prop" passes unchanged
    - Existing test "target is _blank" passes unchanged
    - Existing test "rel is noopener noreferrer" passes unchanged
    - Existing test "renders icon child in the DOM" passes unchanged
    - NEW test "inner button has tabIndex -1 to prevent double focus stop" passes — inner <button> element has attribute tabindex="-1"
    - `yarn test:coverage` exits 0 with 100% coverage on ContactIconButton.tsx
  </behavior>
  <action>
    Step 1 — Update ContactIconButton.tsx: make exactly two changes to the JSX in the return statement.

    Change 1 — Add className to the outer <a> tag. The <a> currently has no className. Add:
    `className="focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 focus-visible:ring-offset-2 rounded-full"`

    This follows the same pattern as NavLink.tsx. `focus-visible:outline-none` suppresses the browser default outline ONLY because it is replaced by the custom ring — never suppress without replacement. `rounded-full` matches the circular Button shape so the ring wraps correctly.

    Change 2 — Add `tabIndex={-1}` to the inner Button. The Button currently is:
    `<Button variant="outline" size="icon" className="h-11 w-11 rounded-full">`
    Change to:
    `<Button variant="outline" size="icon" className="h-11 w-11 rounded-full" tabIndex={-1}>`

    Base UI Button forwards unknown props to the underlying <button> element, so tabIndex will be rendered as the `tabindex` HTML attribute on the DOM <button> element.

    No other changes to the file. The aria-label is correctly on the outer <a> (the focusable element). The rel="noopener noreferrer" and target="_blank" remain unchanged.

    Step 2 — Update ContactIconButton.test.tsx: add one new test at the end of the describe block, after the last existing test ("renders icon child in the DOM").

    New test text: `"inner button has tabIndex -1 to prevent double focus stop"` — render the component, get container.firstChild as HTMLAnchorElement, call anchor.querySelector("button") to get the inner button element, assert `expect(button).toHaveAttribute("tabindex", "-1")`. The attribute name in HTML is lowercase "tabindex" (not "tabIndex").

    The new test must use the same testIcon setup variable already defined at line 6 of the test file: `const testIcon = <span data-testid="test-icon">icon</span>`.

    Run `yarn test:coverage` to verify all 7 tests pass and 100% coverage is maintained.
  </action>
  <verify>
    <automated>cd /home/leforjer/afolarte-portfolio && yarn test:coverage --testPathPattern=ContactIconButton 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 7 tests pass (6 existing + 1 new). `yarn test:coverage` exits 0. ContactIconButton.tsx outer <a> has focus-visible ring classes. Inner Button has tabIndex={-1}.
  </done>
</task>

</tasks>

<verification>
1. `yarn test:coverage --testPathPattern=ContactIconButton` — exits 0, 7 tests pass, 100% coverage
2. `grep -c "tabIndex={-1}" src/components/atoms/ContactIconButton.tsx` — outputs 1
3. `grep -c "focus-visible:ring-2" src/components/atoms/ContactIconButton.tsx` — outputs 1
4. `grep -c "focus-visible:outline-none" src/components/atoms/ContactIconButton.tsx` — outputs 1
5. `grep -c "tabindex" src/components/atoms/ContactIconButton.test.tsx` — outputs at least 1
</verification>

<success_criteria>
- ContactIconButton outer <a> has `className="focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 focus-visible:ring-offset-2 rounded-full"`
- ContactIconButton inner Button has `tabIndex={-1}`
- 7 unit tests pass (6 existing unchanged + 1 new tabindex assertion)
- `yarn test:coverage` exits 0 with 100% coverage on the component
</success_criteria>

<output>
Create `.planning/phases/05-polish/05-02-SUMMARY.md` when done
</output>
