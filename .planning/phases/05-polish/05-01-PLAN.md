---
phase: 05-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/opengraph-image.tsx
autonomous: true
requirements:
  - SEO-01
must_haves:
  truths:
    - "Sharing the portfolio URL on Slack, Twitter, or LinkedIn renders the correct title, description, and preview image"
    - "og:title is 'Andy Olarte — Senior Frontend Engineer'"
    - "og:description matches the portfolio description string"
    - "og:image is auto-resolved by Next.js from the opengraph-image.tsx route"
    - "twitter:card is 'summary_large_image'"
  artifacts:
    - path: "src/app/opengraph-image.tsx"
      provides: "1200x630 PNG OG image served at /opengraph-image"
      exports: ["alt", "size", "contentType", "default Image"]
    - path: "src/app/layout.tsx"
      provides: "metadataBase + openGraph + twitter fields injected into page <head>"
      contains: "metadataBase"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/opengraph-image.tsx"
      via: "Next.js auto-resolves opengraph-image.tsx when metadataBase is set"
      pattern: "metadataBase.*new URL"
    - from: "src/app/opengraph-image.tsx"
      to: "src/lib/heroContent.ts"
      via: "static import of heroContent.name and heroContent.title"
      pattern: "heroContent\\.name|heroContent\\.title"
---

<objective>
Add Open Graph and Twitter Card metadata to layout.tsx and create the opengraph-image.tsx route that generates a 1200×630 PNG OG preview image using Next.js 16's built-in ImageResponse (next/og).

Purpose: When the portfolio URL is shared on Slack, Twitter, or LinkedIn, the preview renders Andy's name, title, and indigo-accented branding — not a blank card.
Output: layout.tsx extended with metadataBase + openGraph + twitter; opengraph-image.tsx route generating a dark-background PNG with name/title/domain.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-RESEARCH.md
@.planning/phases/05-polish/05-PATTERNS.md

<interfaces>
<!-- Current state of files being modified. Read before touching anything. -->

From src/app/layout.tsx (current metadata — lines 18-22):
The existing metadata object has only `title` and `description`. The task extends it with
`metadataBase`, `openGraph`, and `twitter`. JSX of RootLayout is NOT touched.

From src/lib/heroContent.ts:
```typescript
export const heroContent: HeroContent = {
  name: "Andy Francisco Olarte Cardona",
  title: "Senior Frontend / Full-Stack Engineer",
  // ... other fields
};
```

Key constraints (non-negotiable):
- `process.env["NEXT_PUBLIC_SITE_URL"]` — bracket notation REQUIRED (tsconfig noPropertyAccessFromIndexSignature)
- metadataBase fallback REQUIRED: `?? "https://andyolarte.dev"` — missing fallback causes build-time TypeError
- opengraph-image.tsx: ALL styles must use inline style={{}} objects — no className, no Tailwind, no CSS vars
- Only display: "flex" and display: "none" in ImageResponse JSX (Satori limitation)
- Import ImageResponse from "next/og" (NOT "@vercel/og")
- Do NOT export `runtime = "edge"` — default Node.js runtime with static optimization is correct
- Do NOT add `images` array to openGraph or twitter objects — Next.js auto-resolves from opengraph-image.tsx
- Function return type must be explicitly declared: `): ImageResponse`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend layout.tsx metadata with metadataBase, openGraph, twitter</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    src/app/layout.tsx — read the full current file to see the existing metadata object (lines 18-22) before editing
    src/lib/heroContent.ts — verify heroContent.name/title values (used in OG strings for consistency)
    node_modules/next/dist/docs/01-app/03-api-reference/04-functions/generate-metadata.md — Next.js 16 Metadata API
  </read_first>
  <behavior>
    - metadata.metadataBase is `new URL(process.env["NEXT_PUBLIC_SITE_URL"] ?? "https://andyolarte.dev")`
    - metadata.openGraph.type is "website"
    - metadata.openGraph.title is "Andy Olarte — Senior Frontend Engineer"
    - metadata.openGraph.description matches the existing description string exactly
    - metadata.openGraph.url is "/"
    - metadata.twitter.card is "summary_large_image"
    - metadata.twitter.title is "Andy Olarte — Senior Frontend Engineer"
    - metadata.twitter.description matches the existing description string exactly
    - No `images` array in openGraph or twitter (Next.js auto-resolves from opengraph-image.tsx)
    - No changes to RootLayout JSX
  </behavior>
  <action>
    Modify src/app/layout.tsx: extend the existing `export const metadata: Metadata` object (lines 18-22) by adding three new fields after the existing `description` field: `metadataBase`, `openGraph`, and `twitter`.

    metadataBase value: `new URL(process.env["NEXT_PUBLIC_SITE_URL"] ?? "https://andyolarte.dev")` — bracket notation is required by noPropertyAccessFromIndexSignature; the fallback "https://andyolarte.dev" prevents TypeError when the env var is absent.

    openGraph object: `{ type: "website", title: "Andy Olarte — Senior Frontend Engineer", description: "Portfolio of Andy Olarte, Senior Frontend / Full-Stack Engineer with 10+ years of experience building scalable web applications.", url: "/" }` — type must be the string literal "website" (OpenGraphWebsite type); url "/" is relative and resolved by metadataBase.

    twitter object: `{ card: "summary_large_image", title: "Andy Olarte — Senior Frontend Engineer", description: "Portfolio of Andy Olarte, Senior Frontend / Full-Stack Engineer with 10+ years of experience building scalable web applications." }` — card must be the string literal "summary_large_image".

    Do NOT add an `images` array to either openGraph or twitter — Next.js will automatically wire og:image from opengraph-image.tsx when metadataBase is set. Adding images explicitly causes duplicate og:image meta tags.

    The `import type { Metadata } from "next"` import at line 1 already exists — do not duplicate it.

    Verify `yarn build` completes without TypeScript errors after this change. The metadata type system is strict — mistyped literals (e.g., `type: "Website"` instead of `"website"`) cause compile errors.
  </action>
  <verify>
    <automated>cd /home/leforjer/afolarte-portfolio && yarn tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    `yarn tsc --noEmit` exits 0. layout.tsx contains `metadataBase`, `openGraph`, and `twitter` fields with the exact string values specified. No `images` array present in openGraph or twitter.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create opengraph-image.tsx OG image route</name>
  <files>src/app/opengraph-image.tsx</files>
  <read_first>
    node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/opengraph-image.md — file convention, required named exports
    .planning/phases/05-polish/05-PATTERNS.md — full implementation pattern (lines 78-147)
    .planning/phases/05-polish/05-RESEARCH.md — code example (lines 480-529)
    src/lib/heroContent.ts — confirms heroContent.name = "Andy Francisco Olarte Cardona", heroContent.title = "Senior Frontend / Full-Stack Engineer"
  </read_first>
  <behavior>
    - GET /opengraph-image returns HTTP 200 with Content-Type: image/png
    - Rendered PNG is 1200×630px
    - `alt` export equals "Andy Francisco Olarte Cardona — Senior Frontend / Full-Stack Engineer"
    - `size` export is { width: 1200, height: 630 }
    - `contentType` export is "image/png"
    - `export default function Image(): ImageResponse` (explicit return type per project rules)
    - Background is solid #09090b (dark zinc)
    - Left edge has an 8px-wide vertical bar in #6366f1 (indigo-500)
    - Name text "Andy Francisco Olarte Cardona" at 56px bold #ffffff
    - Title text "Senior Frontend / Full-Stack Engineer" at 28px regular #a1a1aa
    - Domain text from NEXT_PUBLIC_SITE_URL (or fallback "andyolarte.dev") at 20px regular #6366f1
    - Content block is vertically centered within 630px height
    - No runtime="edge" export
    - No import from "@vercel/og"
  </behavior>
  <action>
    Create src/app/opengraph-image.tsx as a new file. This file follows the Next.js 16 opengraph-image route file convention — placing it in src/app/ makes Next.js serve it at /opengraph-image and auto-wire it as og:image when metadataBase is set.

    Imports: `import { ImageResponse } from "next/og"` and `import { heroContent } from "@/lib/heroContent"`. No `import type` needed here — ImageResponse is a class, not a type.

    Three required named exports (Next.js file convention):
    - `export const alt = \`${heroContent.name} — ${heroContent.title}\`` — produces "Andy Francisco Olarte Cardona — Senior Frontend / Full-Stack Engineer"
    - `export const size = { width: 1200, height: 630 }`
    - `export const contentType = "image/png"`

    Default export function — must declare return type explicitly per project rules:
    `export default function Image(): ImageResponse`

    Inside the function, read `process.env["NEXT_PUBLIC_SITE_URL"] ?? "andyolarte.dev"` (bracket notation per noPropertyAccessFromIndexSignature; fallback is the plain domain without protocol, displayed as text on the image).

    Return new ImageResponse with JSX structure:
    - Outer div: `style={{ display: "flex", width: "100%", height: "100%", backgroundColor: "#09090b" }}`
    - Left accent div: `style={{ width: 8, backgroundColor: "#6366f1", flexShrink: 0 }}`
    - Content column div: `style={{ display: "flex", flexDirection: "column", justifyContent: "center", paddingLeft: 64, paddingRight: 64, flex: 1 }}`
    - Name p: `style={{ fontSize: 56, fontWeight: 700, color: "#ffffff", margin: 0, lineHeight: 1.1 }}` — content: {heroContent.name}
    - Title p: `style={{ fontSize: 28, fontWeight: 400, color: "#a1a1aa", margin: 0, marginTop: 16, lineHeight: 1.3 }}` — content: {heroContent.title}
    - Domain p: `style={{ fontSize: 20, fontWeight: 400, color: "#6366f1", margin: 0, marginTop: 12, lineHeight: 1.5 }}` — content: {siteUrl}

    Pass `size` as the second argument to `new ImageResponse(jsx, size)`.

    Critical Satori constraints — ALL styles are inline `style={{}}` objects. No className, no Tailwind classes, no CSS variables (Satori does not process them). Only `display: "flex"` — never `display: "block"`. All numeric values (margin, padding, fontSize) are plain numbers (not strings like "64px"). Colors are hex literals — not CSS variables like `var(--primary)`.

    NOTE: src/app/** is excluded from Jest unit coverage (jest.config.mjs `!src/app/**` rule) — this file is covered by E2E tests in Plan 04, not unit tests.
  </action>
  <verify>
    <automated>cd /home/leforjer/afolarte-portfolio && yarn tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    `yarn tsc --noEmit` exits 0. File src/app/opengraph-image.tsx exists and exports `alt`, `size`, `contentType`, and default `Image` function. `yarn build` completes without errors. No className attributes in the ImageResponse JSX tree.
  </done>
</task>

</tasks>

<verification>
1. `yarn tsc --noEmit` — exits 0, no TypeScript errors
2. `yarn build` — exits 0, no build errors (validates metadataBase URL constructor and ImageResponse JSX)
3. `grep -c "metadataBase" src/app/layout.tsx` — outputs 1
4. `grep -c "openGraph" src/app/layout.tsx` — outputs at least 1
5. `grep -c "twitter" src/app/layout.tsx` — outputs at least 1
6. `grep -v "^//" src/app/layout.tsx | grep -c "images"` — outputs 0 (no explicit images array)
7. `grep -c "ImageResponse" src/app/opengraph-image.tsx` — outputs at least 2 (import + usage)
8. `grep -c "className" src/app/opengraph-image.tsx` — outputs 0 (no Tailwind classes in Satori JSX)
9. `grep -c "runtime" src/app/opengraph-image.tsx` — outputs 0 (no edge runtime export)
10. `grep -c "@vercel/og" src/app/opengraph-image.tsx` — outputs 0 (must import from "next/og")
</verification>

<success_criteria>
- layout.tsx metadata object contains metadataBase (with NEXT_PUBLIC_SITE_URL fallback), openGraph (type, title, description, url), and twitter (card, title, description) — no explicit images array
- opengraph-image.tsx exports alt, size (1200×630), contentType ("image/png"), and default Image(): ImageResponse
- All ImageResponse JSX uses inline style={{}} with hex color literals — zero className attributes
- `yarn tsc --noEmit` exits 0
- `yarn build` exits 0 (validates URL constructor and Satori JSX at build time)
</success_criteria>

<output>
Create `.planning/phases/05-polish/05-01-SUMMARY.md` when done
</output>
