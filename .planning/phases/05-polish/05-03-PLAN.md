---
phase: 05-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/atoms/HeroPhoto.tsx
  - src/components/atoms/HeroPhoto.test.tsx
autonomous: false
requirements:
  - LAYOUT-05
user_setup:
  - service: portfolio-photo
    why: "Hero section real photo (LCP candidate) — Next.js Image preload optimization only activates when src is defined"
    required_files:
      - name: public/andy-olarte.jpg
        source: "Place a minimum 640×640px square-cropped JPEG or WebP of your professional headshot at public/andy-olarte.jpg (or public/andy-olarte.webp). The component uses object-top framing so ensure your face is in the upper portion of the image."
must_haves:
  truths:
    - "When a real photo is present at public/andy-olarte.jpg, the HeroPhoto Image emits a <link rel='preload'> in the SSR output (LCP optimization)"
    - "HeroPhoto uses preload={true} (not the deprecated priority prop) per Next.js 16"
    - "The placeholder branch (no src) is unaffected — still renders AO initials"
  artifacts:
    - path: "src/components/atoms/HeroPhoto.tsx"
      provides: "preload={true} on next/image when src is present"
      contains: "preload={true}"
    - path: "src/components/atoms/HeroPhoto.test.tsx"
      provides: "test asserting no priority attribute present (deprecated API guard)"
      contains: "priority"
  key_links:
    - from: "src/components/atoms/HeroPhoto.tsx src-present branch"
      to: "<link rel='preload'> in SSR output"
      via: "preload={true} on next/image"
      pattern: "preload=\\{true\\}"
---

<objective>
Add preload={true} to the HeroPhoto next/image component (the `src`-present branch only) and provide a human checkpoint for Andy to place his professional photo at public/andy-olarte.jpg. This implements the LCP optimization for LAYOUT-05 — the hero photo is the likely Largest Contentful Paint element.

Purpose: When the real photo is loaded, Next.js will emit a <link rel="preload"> tag in the SSR output, telling the browser to start downloading the image before render. This is the primary LCP optimization for LAYOUT-05.
Output: HeroPhoto.tsx with preload={true} added; HeroPhoto.test.tsx updated with deprecated-API guard test; Andy's photo in public/.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-PATTERNS.md

<interfaces>
<!-- Current state of HeroPhoto — read before modifying -->

From src/components/atoms/HeroPhoto.tsx (full file — 25 lines):
```tsx
import type React from "react";
import Image from "next/image";

interface HeroPhotoProps {
  src?: string | undefined;
  alt: string;
  initials: string;
}

export default function HeroPhoto({ src, alt, initials }: HeroPhotoProps): React.ReactNode {
  if (!src) {
    return (
      <div className="relative aspect-square w-full max-w-[320px] rounded-2xl bg-primary ring-2 ring-primary ring-offset-2 ring-offset-background flex items-center justify-center">
        <span className="text-4xl font-bold text-primary-foreground">{initials}</span>
      </div>
    );
  }

  return (
    <div className="relative aspect-square w-full max-w-[320px] rounded-2xl ring-2 ring-primary ring-offset-2 ring-offset-background overflow-hidden">
      <Image src={src} alt={alt} fill className="object-cover object-top" />
    </div>
  );
}
```

Target change (line 22 only):
BEFORE: <Image src={src} alt={alt} fill className="object-cover object-top" />
AFTER:  <Image src={src} alt={alt} fill preload={true} className="object-cover object-top" />

Critical: use `preload={true}` NOT `priority={true}`.
`priority` is deprecated in Next.js 16.0.0 (generates build warning).
`preload` is the Next.js 16 replacement — emits <link rel="preload"> in SSR output.

From src/components/atoms/HeroPhoto.test.tsx (current 3 tests):
1. "renders AO initials placeholder when src is not provided"
2. "renders an img element with alt text when src is provided"
3. "placeholder container has rounded-2xl class"

All 3 pass unchanged. One new test must be added to guard against the deprecated priority prop.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Nothing yet — this checkpoint happens before the code change because the preload optimization only activates when a real photo is present.</what-built>
  <how-to-verify>
    1. Obtain a professional headshot photo (minimum 640×640px square crop, JPEG or WebP)
    2. Place the file at: /home/leforjer/afolarte-portfolio/public/andy-olarte.jpg
       (WebP is preferred for smaller file size — public/andy-olarte.webp is also accepted)
    3. Ensure the face is in the upper portion of the crop — HeroPhoto uses object-top positioning
    4. Confirm the file exists: ls -la /home/leforjer/afolarte-portfolio/public/andy-olarte.jpg
    5. Then update src/lib/heroContent.ts: add the src field to the photo object so HeroPhoto renders the real image:
       photo: { alt: "Andy Francisco Olarte Cardona — profile photo", initials: "AO", src: "/andy-olarte.jpg" }
       (Note: The TypeScript type uses exactOptionalPropertyTypes — the src field must be explicitly set, not left undefined)

    NOTE: The HeroPhoto code change (preload={true}) in the next task is safe to ship regardless of whether the photo exists — the preload prop only activates when src is defined. You can merge the code change now and add the photo later.
  </how-to-verify>
  <resume-signal>Type "photo-added" once the file is at public/andy-olarte.jpg and heroContent.ts is updated, or type "photo-deferred" to ship the preload code change now and add the photo separately.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add preload={true} to HeroPhoto and guard test</name>
  <files>src/components/atoms/HeroPhoto.tsx, src/components/atoms/HeroPhoto.test.tsx</files>
  <read_first>
    src/components/atoms/HeroPhoto.tsx — read full current file before editing (25 lines)
    src/components/atoms/HeroPhoto.test.tsx — read full current test file before editing (21 lines)
    node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md — verify preload prop (not priority)
  </read_first>
  <behavior>
    - Existing test "renders AO initials placeholder when src is not provided" passes unchanged
    - Existing test "renders an img element with alt text when src is provided" passes unchanged
    - Existing test "placeholder container has rounded-2xl class" passes unchanged
    - NEW test: when src is present, the rendered img element does NOT have a `priority` attribute (guards against using the deprecated Next.js 16 API)
    - `yarn test:coverage` exits 0 with 100% coverage on HeroPhoto.tsx
  </behavior>
  <action>
    Step 1 — Update HeroPhoto.tsx: make exactly one change to line 22 (the Image tag in the src-present branch).

    Current line 22: `<Image src={src} alt={alt} fill className="object-cover object-top" />`
    New line 22: `<Image src={src} alt={alt} fill preload={true} className="object-cover object-top" />`

    Add `preload={true}` between `fill` and `className`. This is inside the `if (!src) { return ... }` else branch — the placeholder branch is untouched.

    Do NOT use `priority={true}`. The `priority` prop is deprecated in Next.js 16.0.0 — it still compiles (TypeScript types retain it for backward compatibility) but generates a deprecation warning in build output. The `preload` prop is the Next.js 16 replacement and emits a `<link rel="preload" as="image">` in the SSR `<head>`.

    Step 2 — Update HeroPhoto.test.tsx: add one new test after the last existing test ("placeholder container has rounded-2xl class").

    New test: `"img rendered when src is provided does not use deprecated priority prop"` — render `<HeroPhoto src="/photo.jpg" alt="Andy Olarte" initials="AO" />`, get the img element via `screen.getByAltText("Andy Olarte")`, assert `expect(imgElement).not.toHaveAttribute("priority")`.

    This test guards against future regression back to the deprecated API. The `priority` prop, if used, would not appear as a DOM attribute on the rendered `<img>` — this test documents the expected behavior and prevents accidental reintroduction.

    Run `yarn test:coverage` to verify all 4 tests pass and 100% coverage is maintained.
  </action>
  <verify>
    <automated>cd /home/leforjer/afolarte-portfolio && yarn test:coverage --testPathPattern=HeroPhoto 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 4 tests pass (3 existing + 1 new). `yarn test:coverage` exits 0. HeroPhoto.tsx line 22 contains `preload={true}`. No `priority` prop anywhere in HeroPhoto.tsx.
  </done>
</task>

</tasks>

<verification>
1. `yarn test:coverage --testPathPattern=HeroPhoto` — exits 0, 4 tests pass, 100% coverage
2. `grep -c "preload={true}" src/components/atoms/HeroPhoto.tsx` — outputs 1
3. `grep -c "priority" src/components/atoms/HeroPhoto.tsx` — outputs 0 (no deprecated prop)
4. `grep -c "priority" src/components/atoms/HeroPhoto.test.tsx` — outputs at least 1 (guard test)
5. `yarn tsc --noEmit` — exits 0
</verification>

<success_criteria>
- HeroPhoto.tsx src-present branch uses preload={true} (not priority)
- No `priority` attribute anywhere in HeroPhoto.tsx
- 4 unit tests pass (3 existing + 1 new deprecated-API guard)
- `yarn test:coverage` exits 0 with 100% coverage
- Human checkpoint completed: Andy has placed photo at public/andy-olarte.jpg (or deferred)
</success_criteria>

<output>
Create `.planning/phases/05-polish/05-03-SUMMARY.md` when done
</output>
