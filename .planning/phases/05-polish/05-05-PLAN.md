---
phase: 05-polish
plan: 05
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
files_modified:
  - e2e/home.spec.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
must_haves:
  truths:
    - "E2E tests assert og:title, og:description, og:image meta tags are present in page <head>"
    - "E2E test asserts /opengraph-image route returns HTTP 200 with content-type image/png"
    - "E2E test asserts keyboard Tab navigation reaches NavLink and CTAButton with visible focus rings"
    - "All existing E2E tests continue to pass"
    - "6 OG / Social metadata tests pass covering title, description, type, twitter:card, og:image URL pattern, and route 200"
  artifacts:
    - path: "e2e/home.spec.ts"
      provides: "OG metadata test suite and keyboard navigation accessibility test suite"
      contains: "og:title"
  key_links:
    - from: "e2e/home.spec.ts OG tests"
      to: "src/app/layout.tsx metadata"
      via: "page.locator('meta[property=\"og:title\"]').toHaveAttribute('content', ...)"
      pattern: "og:title"
    - from: "e2e/home.spec.ts keyboard tests"
      to: "ContactIconButton and NavLink focus rings"
      via: "page.keyboard.press('Tab') then page.locator(':focus')"
      pattern: "keyboard"
---

<objective>
Add E2E test suites to home.spec.ts covering: (1) Open Graph meta tag presence and values (SEO-01), (2) /opengraph-image route returns a PNG (SEO-01), and (3) keyboard Tab navigation reaches interactive elements with visible focus rings (SEO-02). These tests run against a live Next.js server and are the only coverage for src/app/opengraph-image.tsx (excluded from Jest unit coverage).

Purpose: Automated verification that OG metadata is correctly injected into the <head> and that keyboard navigation works across the full page — confirms Plans 01 and 02 deliver their requirements end-to-end.
Output: e2e/home.spec.ts with two new test.describe blocks appended after existing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish/05-CONTEXT.md
@.planning/phases/05-polish/05-PATTERNS.md
@.planning/phases/05-polish/05-01-SUMMARY.md
@.planning/phases/05-polish/05-02-SUMMARY.md

<interfaces>
<!-- Current state of e2e/home.spec.ts — read before modifying -->

Current test file structure (296 lines):
- test.describe("Homepage structure") — 3 tests
- test.describe("Smooth scroll navigation") — 3 tests
- test.describe("Dark mode toggle") — 3 tests
- test.describe("Responsive layout") — 5 tests
- test.describe("Navbar transparent/solid transition") — 2 tests
- test.describe("Hero section content") — 6 tests
- test.describe("Hero CTA behavior") — 2 tests
- test.describe("Experience section content") — 5 tests
- test.describe("Skills section content") — 3 tests
- test.describe("Contact section content") — 5 tests

New blocks to APPEND at end of file:
1. test.describe("OG / Social metadata")
2. test.describe("Keyboard navigation accessibility")

From 05-PATTERNS.md (E2E test structure, lines 396-408):
```typescript
test.describe("OG / Social metadata", () => {
  test("og:title meta tag is present", async ({ page }) => {
    await page.goto("/");
    await expect(page.locator('meta[property="og:title"]')).toHaveAttribute(
      "content",
      "Andy Olarte — Senior Frontend Engineer",
    );
  });
});
```

From src/app/layout.tsx (after Plan 01 — the og:title value):
"Andy Olarte — Senior Frontend Engineer"

Contact icon button aria-labels from src/lib/contactContent.ts (verified in research):
Actual values: "Send Andy an email", "Andy's GitHub profile", "Andy's LinkedIn profile"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OG metadata E2E tests</name>
  <files>e2e/home.spec.ts</files>
  <read_first>
    e2e/home.spec.ts — read the full file (296 lines) to see the existing structure and append correctly
    src/app/layout.tsx — read after Plan 01 executes to verify the exact og:title, og:description values
    .planning/phases/05-polish/05-PATTERNS.md — E2E test structure pattern (lines 396-408)
  </read_first>
  <behavior>
    - Test "og:title meta tag has correct content" — page.locator('meta[property="og:title"]') has attribute content "Andy Olarte — Senior Frontend Engineer"
    - Test "og:description meta tag has correct content" — page.locator('meta[property="og:description"]') has attribute content containing "10+ years"
    - Test "og:type is website" — page.locator('meta[property="og:type"]') has attribute content "website"
    - Test "twitter:card is summary_large_image" — page.locator('meta[name="twitter:card"]') has attribute content "summary_large_image"
    - Test "opengraph-image route returns a PNG" — GET /opengraph-image returns status 200 (via page.request.get or page.goto)
    - Test "og:image meta tag content matches opengraph-image route" — page.locator('meta[property="og:image"]') has attribute content matching /opengraph-image/
    - All existing E2E tests still pass
  </behavior>
  <action>
    Append a new `test.describe("OG / Social metadata", ...)` block to the end of e2e/home.spec.ts (after the last existing test block "Contact section content").

    The block must contain 6 tests:

    1. og:title test: `await page.goto("/")` then assert `page.locator('meta[property="og:title"]')` has attribute `content` equal to `"Andy Olarte — Senior Frontend Engineer"`. Note: the locator for og:title uses `property` attribute (not `name`).

    2. og:description test: `await page.goto("/")` then assert `page.locator('meta[property="og:description"]')` has attribute `content` matching `/10\+ years/` (use a regex to avoid matching the exact long string and make the test maintainable).

    3. og:type test: `await page.goto("/")` then assert `page.locator('meta[property="og:type"]')` has attribute `content` equal to `"website"`.

    4. twitter:card test: `await page.goto("/")` then assert `page.locator('meta[name="twitter:card"]')` has attribute `content` equal to `"summary_large_image"`. Note: twitter meta tags use `name` attribute (not `property`).

    5. /opengraph-image route test: use `const response = await page.request.get("/opengraph-image")` then `expect(response.status()).toBe(200)` then `expect(response.headers()["content-type"]).toContain("image/png")`.

    6. og:image URL pattern test: `await page.goto("/")` then assert `page.locator('meta[property="og:image"]')` has attribute `content` matching the regex `/opengraph-image/`. This verifies that the `og:image` tag is injected and points to the route-based OG image file. Note: the attribute value will be an absolute URL (e.g. `https://andyolarte.dev/opengraph-image`) or a relative path depending on the `NEXT_PUBLIC_SITE_URL` env var — use a regex match to avoid brittle full-URL assertions.

    Important: `test.describe` does not need `test.use({ viewport: ... })` for these tests since they don't test visual layout. The metadata tests just check the DOM.

    Playwright `toHaveAttribute` for meta tags: the locator `page.locator('meta[property="og:title"]')` matches the first meta element with that property attribute. Use `await expect(...).toHaveAttribute("content", expectedValue)` directly — Playwright handles the async await pattern.

    Do NOT use `page.evaluate` to read meta content — use the Playwright locator API for consistent async handling.
  </action>
  <verify>
    <automated>cd /home/leforjer/afolarte-portfolio && yarn test:e2e --grep "OG / Social metadata" 2>&1 | tail -20</automated>
  </verify>
  <done>
    6 OG tests pass (og:title, og:description, og:type, twitter:card, opengraph-image route 200, og:image URL pattern). All existing E2E tests continue to pass. `yarn test:e2e` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add keyboard navigation accessibility E2E tests</name>
  <files>e2e/home.spec.ts</files>
  <read_first>
    e2e/home.spec.ts — read the current file state after Task 1 edits to find the correct position to append
    .planning/phases/05-polish/05-CONTEXT.md — D-08 audit scope
    .planning/phases/05-polish/05-RESEARCH.md — Phase Requirements table for SEO-02
  </read_first>
  <behavior>
    - Test "NavLink is reachable via keyboard Tab from top of page" — tabbing from page start reaches a nav link
    - Test "CTAButton is reachable via keyboard Tab" — the "Get in touch" button is reachable via Tab
    - Test "ContactIconButton single focus stop" — each ContactIconButton anchor is reachable via Tab without an intermediate stop on the inner button
    - Test "ThemeToggle is reachable via keyboard Tab" — the theme toggle button is reachable
    - All existing E2E tests still pass
  </behavior>
  <action>
    Append a second new `test.describe("Keyboard navigation accessibility", ...)` block after the OG metadata block added in Task 1.

    Use `test.use({ viewport: { width: 1280, height: 720 } })` inside the describe block — desktop viewport so desktop nav links are visible.

    Test 1 — NavLink keyboard reach: `await page.goto("/")`. Focus the body: `await page.locator("body").focus()`. Press Tab multiple times until a nav link is focused. Use: `await page.keyboard.press("Tab")` in a loop, then assert `await expect(page.locator("a[href='#experience']").first()).toBeFocused()` after enough Tab presses. A simpler approach: after `page.goto("/")`, use `await page.keyboard.press("Tab")` and check `await page.locator(":focus").getAttribute("href")` is not null and contains "#". The exact number of tabs depends on the DOM order — inspect page.locator(":focus") rather than assuming a fixed tab count.

    Practical approach for keyboard Tab tests: Use `page.keyboard.press("Tab")` then `await expect(page.locator("a[href='#experience']").first()).toBeFocused()` after navigating to the correct tab position. Since DOM order can shift, a more robust approach: focus the page via `await page.getByRole("link", { name: "Experience" }).first().focus()` and assert `await expect(page.getByRole("link", { name: "Experience" }).first()).toBeFocused()`. Then verify the element is accessible. This tests that the element is keyboard-focusable (reachable) without depending on exact tab stop count.

    Test 2 — CTAButton keyboard reach: `await page.getByRole("button", { name: "Get in touch" }).focus()` then `await expect(page.getByRole("button", { name: "Get in touch" })).toBeFocused()`.

    Test 3 — ContactIconButton single focus stop: This test verifies that after focusing the email anchor (`page.getByRole("link", { name: "Send Andy an email" })`), pressing Tab does NOT land on the inner button before moving to the next anchor. Approach: focus the "Send Andy an email" anchor, then press Tab once, then assert the focused element is NOT the inner button of the same anchor (it should be the GitHub anchor or another element). Use: `await page.getByRole("link", { name: "Send Andy an email" }).focus()`, then `await page.keyboard.press("Tab")`, then `await expect(page.getByRole("link", { name: "Andy's GitHub profile" })).toBeFocused()`. This confirms a single tab stop between consecutive contact buttons (no hidden intermediate stop).

    Test 4 — ThemeToggle keyboard reach: `await page.getByRole("button", { name: "Toggle dark mode" }).focus()` then `await expect(page.getByRole("button", { name: "Toggle dark mode" })).toBeFocused()`.

    Note: `.focus()` on a Playwright locator programmatically focuses the element — this tests that the element CAN be focused (is in the tab order), not that it happens to be at a specific tab count. `toBeFocused()` asserts the element has the `:focus` pseudo-class.
  </action>
  <verify>
    <automated>cd /home/leforjer/afolarte-portfolio && yarn test:e2e --grep "Keyboard navigation accessibility" 2>&1 | tail -20</automated>
  </verify>
  <done>
    4 keyboard navigation tests pass. Full `yarn test:e2e` exits 0 (all suites pass including all previously-existing tests). home.spec.ts contains both "OG / Social metadata" and "Keyboard navigation accessibility" describe blocks.
  </done>
</task>

</tasks>

<verification>
1. `yarn test:e2e` — exits 0, all test suites pass
2. `grep -c "og:title" e2e/home.spec.ts` — outputs at least 1
3. `grep -c "og:description" e2e/home.spec.ts` — outputs at least 1
4. `grep -c "twitter:card" e2e/home.spec.ts` — outputs at least 1
5. `grep -c "opengraph-image" e2e/home.spec.ts` — outputs at least 1
6. `grep -c "toBeFocused" e2e/home.spec.ts` — outputs at least 3 (keyboard nav tests)
7. Total test count in file increases from existing tests — grep -c "test(" e2e/home.spec.ts should be higher than before
</verification>

<success_criteria>
- 6 OG metadata tests pass: og:title, og:description, og:type, twitter:card, opengraph-image route 200, og:image URL pattern matching /opengraph-image/
- 4 keyboard navigation tests pass: NavLink, CTAButton, ContactIconButton single focus stop, ThemeToggle
- All previously-passing E2E tests continue to pass
- `yarn test:e2e` exits 0
</success_criteria>

<output>
Create `.planning/phases/05-polish/05-05-SUMMARY.md` when done
</output>
