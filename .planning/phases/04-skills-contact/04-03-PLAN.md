---
phase: 04-skills-contact
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/atoms/ContactIconButton.tsx
  - src/components/atoms/ContactIconButton.test.tsx
autonomous: true
requirements:
  - CONTACT-01
  - CONTACT-02
  - CONTACT-03

must_haves:
  truths:
    - "ContactIconButton renders an <a> element as root (not a button)"
    - "href, aria-label, target='_blank', rel='noopener noreferrer' are all present on the <a>"
    - "icon prop (React.ReactNode) renders inside the anchor"
    - "44px minimum touch target (h-11 w-11) present on the wrapped Button"
    - "100% test coverage achieved"
  artifacts:
    - path: "src/components/atoms/ContactIconButton.tsx"
      provides: "Icon link atom for contact row — renders <a> wrapping shadcn Button outline/icon"
      exports: ["default ContactIconButton"]
    - path: "src/components/atoms/ContactIconButton.test.tsx"
      provides: "100% coverage for ContactIconButton"
  key_links:
    - from: "src/components/atoms/ContactIconButton.tsx"
      to: "src/components/ui/button"
      via: "import { Button } from '@/components/ui/button'"
      pattern: "import.*Button.*from.*@/components/ui/button"
---

<objective>
Create the ContactIconButton atom — an anchor element wrapping a shadcn Button that renders icon-only contact links for the Mail, GitHub, and LinkedIn buttons in the contact section.

Purpose: ContactSection (Plan 04-05) renders three ContactIconButton instances. This atom provides keyboard-navigable, accessible icon links with proper WCAG touch targets.
Output: ContactIconButton.tsx + ContactIconButton.test.tsx with 100% coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-skills-contact/04-UI-SPEC.md
@.planning/phases/04-skills-contact/04-PATTERNS.md

<interfaces>
<!-- Analog: src/components/atoms/CTAButton.tsx (role-match) -->
<!-- Key difference: ContactIconButton uses <a> not <button>, no "use client", no onClick -->

From src/components/atoms/CTAButton.tsx:
```typescript
"use client";
import { Button } from "@/components/ui/button";
interface CTAButtonProps { label: string; targetId: string; }
export default function CTAButton({ label, targetId }: CTAButtonProps): React.ReactNode {
  return (
    <Button variant="default" size="default" onClick={handleClick} className="min-h-11">
      {label}
    </Button>
  );
}
```

ContactIconButton props (from UI-SPEC §4.5):
```typescript
interface ContactIconButtonProps {
  href: string;
  ariaLabel: string;
  icon: React.ReactNode;
}
```

Visual spec (from UI-SPEC §4.5):
- Root element: <a href={href} aria-label={ariaLabel} target="_blank" rel="noopener noreferrer">
- Inner: <Button variant="outline" size="icon" className="h-11 w-11 rounded-full">
- Icon element passed as children: {icon}
- Hover/focus: inherited from shadcn Button outline variant
- NO "use client" directive — no React event handlers, pure static anchor
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ContactIconButton atom</name>
  <files>src/components/atoms/ContactIconButton.tsx, src/components/atoms/ContactIconButton.test.tsx</files>
  <read_first>
    src/components/atoms/CTAButton.tsx — shadcn Button import path, size and variant prop names, className usage
    src/components/atoms/EntryBadge.test.tsx — test file structure baseline (no React import at top for test files)
    src/components/atoms/EntryBadge.tsx — import type React pattern and explicit return type
    .planning/phases/04-skills-contact/04-UI-SPEC.md — §4.5 for props interface and visual spec, §7.2 for interaction states, §9 for accessibility requirements
    .planning/phases/04-skills-contact/04-PATTERNS.md — ContactIconButton.tsx and ContactIconButton.test.tsx sections
  </read_first>
  <behavior>
    - Root element is an <a> (container.firstChild?.nodeName === "A")
    - <a> element has href attribute equal to the prop value
    - <a> element has aria-label attribute equal to the ariaLabel prop value
    - <a> element has target="_blank"
    - <a> element has rel="noopener noreferrer"
    - icon prop (React.ReactNode) is rendered as child content (visible in the DOM)
    - No client-side JavaScript event handlers on the component
  </behavior>
  <action>
    Write ContactIconButton.test.tsx FIRST (TDD red phase). The test file must:
    - Import: "import { render, screen } from "@testing-library/react"" (no top-level React import — no jest.mock with JSX in this file)
    - Describe block: "ContactIconButton"
    - Helper: create a const testIcon using a plain span element: const testIcon = <span data-testid="test-icon">icon</span>
    - Test: "root element is an anchor" — container.firstChild?.nodeName toBe "A"
    - Test: "href attribute matches prop" — render with href="mailto:test@test.com", query container.firstChild as HTMLAnchorElement, expect it toHaveAttribute "href" with "mailto:test@test.com"
    - Test: "aria-label attribute matches ariaLabel prop" — expect anchor toHaveAttribute "aria-label" with "Test label"
    - Test: "target is _blank" — expect anchor toHaveAttribute "target" with "_blank"
    - Test: "rel is noopener noreferrer" — expect anchor toHaveAttribute "rel" with "noopener noreferrer"
    - Test: "renders icon child in the DOM" — pass testIcon as icon prop, expect screen.getByTestId("test-icon") toBeInTheDocument
    Confirm tests fail.

    Then write ContactIconButton.tsx (TDD green phase):
    - No "use client" directive (pure static anchor — no event handlers)
    - "import type React from "react"" (type-only)
    - "import { Button } from "@/components/ui/button"" (value import — Button is a component, not a type)
    - Props interface ContactIconButtonProps: href string, ariaLabel string, icon React.ReactNode
    - Return an <a> element with href={href}, aria-label={ariaLabel}, target="_blank", rel="noopener noreferrer"
    - Inside the <a>: <Button variant="outline" size="icon" className="h-11 w-11 rounded-full">{icon}</Button>
    - Export default function ContactIconButton with explicit : React.ReactNode return type
    - asChild prop is NOT used — the <a> wraps Button as a child, not as a Radix slot
  </action>
  <verify>
    <automated>cd /home/leforjer/afolarte-portfolio && yarn test --testPathPattern="ContactIconButton" --coverage 2>&1 | tail -30</automated>
  </verify>
  <done>
    All ContactIconButton tests pass. Coverage shows 100% for ContactIconButton.tsx. yarn type-check passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Static portfolio — no user input, no auth, no backend. No meaningful attack surface for this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-SC | Tampering | npm installs | accept | No new packages installed in this plan |
| T-04-03-01 | Information Disclosure | External links | accept | All hrefs are hardcoded static values in contactContent.ts — no user-supplied input reaches href attributes |
</threat_model>

<verification>
```bash
cd /home/leforjer/afolarte-portfolio
yarn test --testPathPattern="ContactIconButton" --coverage
yarn type-check
```

Expected: all tests pass, 100% coverage, 0 type errors.
</verification>

<success_criteria>
- ContactIconButton.tsx renders <a> root wrapping shadcn Button with variant="outline" size="icon"
- <a> element receives href, aria-label, target="_blank", rel="noopener noreferrer" from props
- icon prop renders as child inside the Button
- No "use client" directive (Server Component compatible)
- 100% test coverage for ContactIconButton.tsx
- yarn type-check passes with 0 errors
</success_criteria>

<output>
Create `.planning/phases/04-skills-contact/04-03-SUMMARY.md` when done.
</output>
