---
phase: 04-skills-contact
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/atoms/SkillBadge.tsx
  - src/components/atoms/SkillBadge.test.tsx
autonomous: true
requirements:
  - SKILLS-02
  - SKILLS-03

must_haves:
  truths:
    - "SkillBadge renders a span element (not interactive) with the correct label text"
    - "Primary variant renders filled background per domain hue (e.g., bg-indigo-100 text-indigo-700 for frontend)"
    - "Secondary variant renders outline border per domain hue (e.g., border-indigo-400 text-indigo-600 for frontend) with no fill"
    - "All 5 domains and both variants render correctly (10 combinations total — all branches covered)"
    - "100% test coverage achieved"
  artifacts:
    - path: "src/components/atoms/SkillBadge.tsx"
      provides: "SkillBadge atom with domain + variant props"
      exports: ["default SkillBadge"]
    - path: "src/components/atoms/SkillBadge.test.tsx"
      provides: "100% branch coverage for all 5 domains × 2 variants"
  key_links:
    - from: "src/components/atoms/SkillBadge.tsx"
      to: "src/types/skills.ts"
      via: "import type { SkillDomain } from '@/types/skills'"
      pattern: "import type.*SkillDomain.*from.*@/types/skills"
---

<objective>
Create the SkillBadge atom — the leaf-level UI primitive for the skills section. Each badge is a fully rounded pill displaying a skill name with domain-specific color, either filled (primary) or outlined (secondary).

Purpose: SkillBadge is consumed by SkillGroup molecule (Plan 04-04) and provides the visual expert/secondary distinction required by SKILLS-03.
Output: SkillBadge.tsx + SkillBadge.test.tsx with 100% branch coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-skills-contact/04-UI-SPEC.md
@.planning/phases/04-skills-contact/04-PATTERNS.md

<interfaces>
<!-- Analog: src/components/atoms/EntryBadge.tsx (exact match) -->

From src/components/atoms/EntryBadge.tsx:
```typescript
import type React from "react";

interface EntryBadgeProps {
  label: string;
}

export default function EntryBadge({ label }: EntryBadgeProps): React.ReactNode {
  return (
    <span className="inline-flex items-center rounded-full border border-primary px-2.5 py-0.5 text-xs font-medium text-primary">
      {label}
    </span>
  );
}
```

From src/components/atoms/EntryBadge.test.tsx (test pattern):
```typescript
import { render, screen } from "@testing-library/react";
import EntryBadge from "./EntryBadge";

describe("EntryBadge", () => {
  it("renders the label string passed as prop", () => {
    render(<EntryBadge label="Tech Lead" />);
    expect(screen.getByText("Tech Lead")).toBeInTheDocument();
  });
  it("root element is a span", () => {
    const { container } = render(<EntryBadge label="Tech Lead" />);
    expect(container.firstChild?.nodeName).toBe("SPAN");
  });
  it("root span has the rounded-full class", () => {
    const { container } = render(<EntryBadge label="Tech Lead" />);
    const root = container.firstChild as HTMLElement;
    expect(root).toHaveClass("rounded-full");
  });
});
```

Target SkillBadge props (from UI-SPEC §4.1):
```typescript
type SkillDomain = "frontend" | "backend" | "mobile" | "devops" | "testing";
type SkillVariant = "primary" | "secondary";

interface SkillBadgeProps {
  label: string;
  domain: SkillDomain;
  variant: SkillVariant;
}
```

Domain class lookup (static strings — Tailwind purge safety — from PATTERNS.md):
frontend primary:   "bg-indigo-100 text-indigo-700 dark:bg-indigo-950 dark:text-indigo-300"
frontend secondary: "border-indigo-400 text-indigo-600 dark:border-indigo-600 dark:text-indigo-400"
backend primary:    "bg-emerald-100 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-300"
backend secondary:  "border-emerald-400 text-emerald-600 dark:border-emerald-600 dark:text-emerald-400"
mobile primary:     "bg-violet-100 text-violet-700 dark:bg-violet-950 dark:text-violet-300"
mobile secondary:   "border-violet-400 text-violet-600 dark:border-violet-600 dark:text-violet-400"
devops primary:     "bg-amber-100 text-amber-700 dark:bg-amber-950 dark:text-amber-300"
devops secondary:   "border-amber-400 text-amber-600 dark:border-amber-600 dark:text-amber-400"
testing primary:    "bg-rose-100 text-rose-700 dark:bg-rose-950 dark:text-rose-300"
testing secondary:  "border-rose-400 text-rose-600 dark:border-rose-600 dark:text-rose-400"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SkillBadge atom</name>
  <files>src/components/atoms/SkillBadge.tsx, src/components/atoms/SkillBadge.test.tsx</files>
  <read_first>
    src/components/atoms/EntryBadge.tsx — root element type, import pattern, return type signature
    src/components/atoms/EntryBadge.test.tsx — test file structure, coverage pattern
    src/types/skills.ts — SkillDomain type created in Plan 04-01
    .planning/phases/04-skills-contact/04-UI-SPEC.md — §4.1 for props interface and visual spec, §3 for full color table
    .planning/phases/04-skills-contact/04-PATTERNS.md — domainClasses lookup object with all 10 static class strings
  </read_first>
  <behavior>
    - label prop renders as visible text inside the span
    - Primary variant: span has "rounded-full border-0 px-3 py-1 text-sm font-normal" plus domain-specific filled classes
    - Secondary variant: span has "rounded-full border px-2.5 py-0.5 text-xs font-normal bg-transparent" plus domain-specific outline classes
    - Root element is a span for all combinations
    - rounded-full class present on root span for all combinations
    - No onClick or interactive event handlers — badges are informational only
    - All 5 domains × 2 variants produce distinct class strings (10 branches total)
  </behavior>
  <action>
    Write SkillBadge.test.tsx FIRST (TDD red phase). The test file must:
    - Use "import { render, screen } from "@testing-library/react"" (no React import — configured globally)
    - Describe block: "SkillBadge"
    - Test: "renders label text" — render primary frontend, expect screen.getByText("React") toBeInTheDocument
    - Test: "root element is a span" — container.firstChild?.nodeName toBe "SPAN"
    - Test: "has rounded-full class" — container.firstChild as HTMLElement toHaveClass "rounded-full"
    - Test: "primary variant has no border class" — render with variant="primary" domain="frontend", check root does NOT toHaveClass "border" (bare border class, not border-* color classes)
    - Test: "secondary variant has border class" — render with variant="secondary" domain="frontend", check root toHaveClass "border"
    - Test: "primary frontend has bg-indigo-100 class" — toHaveClass "bg-indigo-100"
    - Test: "secondary frontend has border-indigo-400 class" — toHaveClass "border-indigo-400"
    - Test: "primary backend has bg-emerald-100 class" — render domain="backend" variant="primary", toHaveClass "bg-emerald-100"
    - Test: "primary mobile has bg-violet-100 class" — domain="mobile" variant="primary"
    - Test: "primary devops has bg-amber-100 class" — domain="devops" variant="primary"
    - Test: "primary testing has bg-rose-100 class" — domain="testing" variant="primary"
    Confirm tests fail (no implementation yet).

    Then write SkillBadge.tsx (TDD green phase):
    - "import type React from "react"" (type-only, no value import)
    - "import type { SkillDomain } from "@/types/skills"" (type-only, @/ alias)
    - Local type alias: type SkillVariant = "primary" | "secondary"
    - Props interface SkillBadgeProps: label string, domain SkillDomain, variant SkillVariant
    - domainClasses const of type Record<SkillDomain, { primary: string; secondary: string }> — hardcode all 10 class strings as complete static strings (no template literals, no concatenation — Tailwind purge requires full class strings present in source)
    - variantBase const: primary value is "rounded-full border-0 px-3 py-1 text-sm font-normal", secondary value is "rounded-full border px-2.5 py-0.5 text-xs font-normal bg-transparent"
    - Return a span element: className is a template literal combining variantBase[variant] and domainClasses[domain][variant] separated by a space
    - Label is rendered as {label} inside the span
    - Export default function SkillBadge with explicit : React.ReactNode return type

    Note: SkillDomain is imported from @/types/skills. Do NOT re-declare it locally.
    Note: The type SkillVariant is local to this file (it is not exported from skills.ts).
  </action>
  <verify>
    <automated>cd /home/leforjer/afolarte-portfolio && yarn test --testPathPattern="SkillBadge" --coverage 2>&1 | tail -30</automated>
  </verify>
  <done>
    All SkillBadge tests pass. Coverage report shows 100% branches/functions/lines/statements for SkillBadge.tsx. yarn type-check passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Static portfolio — no user input, no auth, no backend. No meaningful attack surface for this plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-SC | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
```bash
cd /home/leforjer/afolarte-portfolio
yarn test --testPathPattern="SkillBadge" --coverage
yarn type-check
```

Expected: all tests pass, 100% coverage, 0 type errors.
</verification>

<success_criteria>
- SkillBadge.tsx exists with SkillDomain imported from @/types/skills, domainClasses lookup with 10 complete static Tailwind class strings, span root element, explicit React.ReactNode return type
- SkillBadge.test.tsx tests all 5 domains and both variants with at least one class assertion per
- 100% branch/function/line/statement coverage for SkillBadge.tsx
- yarn type-check passes with 0 errors
</success_criteria>

<output>
Create `.planning/phases/04-skills-contact/04-02-SUMMARY.md` when done.
</output>
